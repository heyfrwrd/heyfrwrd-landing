---
export interface Props {
  for?: string;
  class?: string;
}
const { for: htmlFor, class: className = "" } = Astro.props as Props;
---

<label
  for={htmlFor}
  class={`block text-sm font-medium text-gray-700 ${className}`}
>
  <slot />
</label>
